# Override cho môi trường dev local
# Khi dùng Neon: tạo thêm 1 "branch" riêng cho dev trong Neon Console
# rồi set DB_HOST trỏ tới pooler endpoint của branch dev
spring.jpa.hibernate.ddl-auto=validate

# Log debug
logging.level.com.shoppingagent=DEBUG
logging.level.org.hibernate.SQL=DEBUG
